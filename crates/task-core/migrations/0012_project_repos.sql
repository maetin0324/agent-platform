-- Migration 12 (schema_version=12): ADR-0043 A1（案件の複数リポジトリ）の**場所取り**。
--
-- この Phase（53 / ADR-0044 B1）は migration 0013 を使うと決まっており、12 は ADR-0043 A1 を
-- 実装する別の枝が持つ。両方を同時に工事しているあいだ、この枝だけで `SCHEMA_VERSION = 13` まで
-- migrate できるように、**中身の無い**（SQL を 1 文も含まない）ファイルを置いておく。
--
-- **マージのときはこのファイルを捨て、ADR-0043 A1 側の 0012_project_repos.sql をそのまま採る。**
-- 本番の DB は統合後のバイナリで初めて 12 に上がるので、この空版が本番に適用されることはない。
